import { MessageResponseDto } from './message-response-dto';

describe('MessageResponseDto', () => {
  it('should create an instance', () => {
    expect(new MessageResponseDto()).toBeTruthy();
  });
});
