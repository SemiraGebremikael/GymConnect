import { GetConversationRequestDto } from './get-conversation-request-dto';

describe('GetConversationRequestDto', () => {
  it('should create an instance', () => {
    expect(new GetConversationRequestDto()).toBeTruthy();
  });
});
