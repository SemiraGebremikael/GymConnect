import { MessageRequestDto } from './message-request-dto';

describe('MessageRequestDto', () => {
  it('should create an instance', () => {
    expect(new MessageRequestDto()).toBeTruthy();
  });
});
